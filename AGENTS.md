## Nix Shell

A `shell.nix` is provided at the project root for a reproducible Python 3 environment.

### Entering the shell

```bash
nix-shell
```

Once inside, `python3` will be available. To exit the shell, press `Ctrl-D` or run `exit`.
